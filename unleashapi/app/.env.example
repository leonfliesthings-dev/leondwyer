# Copy to .env and fill in. .env is gitignored so your token never gets committed.
UL_PAT=ul_pat_your_token_here

# Optional: satellite basemap defaults to Esri World Imagery (no key needed).
# Set a Mapbox token to use Mapbox satellite tiles instead (higher quality).
# MAPBOX_TOKEN=pk.your_mapbox_token
